{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "usecaseCoachMcpEndpoint": {
      "value": "${USECASE_COACH_MCP_ENDPOINT}"
    },
    "catalogReadersPrincipalId": {
      "value": "${CATALOG_READERS_PRINCIPAL_ID}"
    },
    "portalEntraClientId": {
      "value": "${PORTAL_ENTRA_CLIENT_ID}"
    },
    "portalAllowAnonymousAccess": {
      "value": "${PORTAL_ALLOW_ANONYMOUS_ACCESS=false}"
    },
    "agentSkillsRepositoryUrl": {
      "value": "${AGENT_SKILLS_REPOSITORY_URL}"
    }
  }
}
